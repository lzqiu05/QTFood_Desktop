package com.example.qtfood_desktop.BD;

public interface Consultas {
    // Consultas sobre  la tabla de Usuarios
    String LOGIN = "SELECT * FROM Usuarios WHERE Nombre = ? AND Password = ?";

    // Consultas sobre la tabla de Productos


    // Consultas sobre la tabla de Pedidos


    // Consultas sobre la tabla de Ofertas


    // Consultas sobre la tabla de Reservas


    // Consultas sobre la tabla de detalles_pedidos


    // Consultas sobre la tabla de Categorías
}
